[pytest]
testpaths = tests
pythonpath = .
addopts = -v -m "not e2e"
markers =
    unit: fast tests without Selenium
    integration: selenium tests with local HTML
    e2e: real browser tests against live ticket pages