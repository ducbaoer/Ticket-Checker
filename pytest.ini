[pytest]
testpaths = tests
addopts = -v --strict-markers -m "not e2e"
markers =
    e2e: tests that hit real ticket.io pages
    integration: selenium tests with local HTML
    unit: fast parsing tests